---
import JobComponent from "./Job.astro";
import { getJobs } from "@/api/works";

var jobs = await getJobs();
---

<div class="grid grid-cols-1 sm:grid-cols-2 w-full h-fit gap-4">
  {jobs.map((job) => <JobComponent job={job} />)}
</div>
